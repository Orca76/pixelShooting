using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gifExplain : MonoBehaviour
{
    // Start is called before the first frame update
    public string Explain;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
